import React from 'react';

function App() {
  return (
    <div className="min-h-screen flex items-center justify-center dark" style={{ background: 'var(--color-bg-base)', color: 'var(--color-text-primary)' }}>
      <p>Start prompting (or editing) to see magic happen :)</p>
    </div>
  );
}

export default App;
